const getInput = require("./get-input.js");

// console.log to print out a greeting to the user
//1. get the user's name and save it in a variable
const str = getInput();

//3. print out what's in that variable
console.log ("Hello " + str);